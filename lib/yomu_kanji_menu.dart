import 'package:flutter/material.dart';

class YomuKanjiMenu extends StatelessWidget{

  const YomuKanjiMenu({Key key}): super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(
      title: Text('Hi i m the menu'),
      ),
      body: Column(),
    );
  }

}